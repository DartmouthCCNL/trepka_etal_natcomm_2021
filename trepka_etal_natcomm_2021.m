%% plot all figures for trepka et al. 2021
% -figures are plotted from intermediate output data
% saved in the output directory
% -original data is available in datasets folder
addpath(genpath(pwd))
analyze_behavior_complete(0);
analyze_models_complete(0);
analyze_block_simulations_complete(0);
